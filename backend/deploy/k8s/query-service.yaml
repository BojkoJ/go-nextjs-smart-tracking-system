apiVersion: v1
kind: Service
metadata:
  name: query-service
  namespace: tracking-system
spec:
  selector:
    app: query
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      name: http