apiVersion: v1
kind: Service
metadata:
  name: ingest-service
  namespace: tracking-system
spec:
  selector:
    app: ingest
  ports:
    - protocol: TCP
      port: 50051
      targetPort: 50051
      name: grpc